%section.admin_payments
  .container
    .row
      .col-sm-10.col-sm-offset-1
        %section.payment_history
          %header.page_header
            %h1 Orders
          %table.table
            %thead
              %tr
                %th Order ID
                %th Type
                %th Customer Last Name
                %th Customer Email
                %th Shipping State
                %th Shipping Status
            %tbody
              - @orders.each do |order|
                %tr
                  %td.order_id= order.id
                  %td.type= order.type
                  %td.customer_last_name= order.customer_last_name
                  %td.customer_email= order.customer_email
                  %td.shipping_state= order.shipping_state
                  %td.shipping_status= order.shipping_status
